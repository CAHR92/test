SELECT COUNT(DISTINCT FIIDTIENDA) "TIENDAS_TOTAL" FROM CHATBOT.tatiendas;

SELECT COUNT(DISTINCT FCTIPO) "TIPOS_CANTIDAD" FROM CHATBOT.tatiendas;

SELECT fctipo FROM CHATBOT.tatiendas;


SELECT fctipo, COUNT(DISTINCT FIIDTIENDA) from chatbot.tatiendas;


select tablespace_name from dba_tablespaces;
ALTER SESSION SET CONTAINER = CDB$ROOT;
ALTER SESSION SET container = cdb$root;
SELECT * FROM dba_users;

select * from CHATBOT.tausuarios;

DESCRIBE CHATBOT.tausuarios;


desc CHATBOT.tausuarios;

ALTER TABLE CHATBOT.tausuarios RENAME COLUMN FIRECIPIENTID TO FCTOKEN;


 alter table
   CHATBOT.tausuarios
rename column
   FIRECIPIENTID 
TO
   FCTOKEN;
   
   
alter table 
   CHATBOT.tausuarios
modify 
( 
   FCTOKEN    varchar2(512 CHAR)
);